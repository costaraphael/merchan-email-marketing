$("#lista_<%= @lista.id %>.lista-destinatarios")
        .html("<%= escape_javascript(render partial: 'listagem_destinatarios', locals: { lista: @lista } ) %>");

$("#lista_<%= @lista.id %>.lista-destinatarios").find('table').not('.fc-header').dataTable({
    aoColumnDefs: [
        {
            aTargets: [-1],
            bSortable: false,
            bSearchable: false
        }
    ]
});

$('.selectpicker').selectBoxIt();
