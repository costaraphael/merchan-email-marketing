<%= simple_form_for(@campanha) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome %>
    <%= f.association :criador %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
